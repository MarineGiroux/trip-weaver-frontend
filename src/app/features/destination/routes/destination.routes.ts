import { Routes } from '@angular/router';
import { DestinationComponent } from '../page/destination.component';

export const routes: Routes = [
  {
    path: '',
    component: DestinationComponent,
  },
];
